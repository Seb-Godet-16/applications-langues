# 📊 BILAN TECHNIQUE — VACHÉBO
*Application bilingue Français ↔ Espagnol — Juin 2026*
*Métriques revérifiées le 28/08/2026 (voir § 8 Historique)*

---

## 1. Vue d'ensemble

| Critère | Valeur |
|---|---|
| Architecture | SPA (Single Page App) — Vanilla JS ES2020, zéro dépendance |
| Hébergement | GitHub Pages (statique, HTTPS, gratuit) |
| CI/CD | GitHub Actions — déploiement automatique sur push `main` |
| Poids total source | ~889,4 Ko (17 076 lignes de code, hors manifest/deploy) |
| Chargement initial | ~623,5 Ko (app.js + style.css + index.html), les données chargées à la demande |

---

## 2. Métriques de code

| Fichier | Lignes | Taille | Rôle |
|---|---|---|---|
| `app.js` | 6 353 | 304,0 Ko | Moteur applicatif — 188 fonctions nommées de premier niveau, 21 sections |
| `style.css` | 5 687 | 207,4 Ko | Thèmes, composants, animations — 51 variables CSS uniques (179 déclarations, redéfinies par thème/variante) |
| `data-fr.js` | 1 663 | 119,2 Ko | Données mode Français (37 thèmes + 16 dialogues) |
| `data-es.js` | 1 038 | 115,2 Ko | Données mode Espagnol (37 thèmes + 16 dialogues) |
| `index.html` | 1 793 | 114,8 Ko | Structure HTML (4 écrans + 2 modales) |
| `sw.js` | 582 | 31,5 Ko | Service Worker (cache hybride + 4 fallbacks SVG) |
| `manifest.json` | — | — | PWA (11 icônes, portrait, standalone) |
| `deploy.yml` | — | — | CI/CD GitHub Actions |

---

## 3. Architecture applicative

### Navigation (4 écrans)
```
[0] app-launcher   → Choix de langue + sélecteur de variante régionale
[1] home           → Guide utilisateur bilingue (FR/ES, 8 sections accordéon)
[2] sections-L1    → Grille des 37 thèmes de vocabulaire
    sections-L2    → Grille des 16 dialogues
[3] lesson         → Leçon ouverte — onglets Flash / Vocab / Quiz / Répète / Dialogue
```
**Ajouté le 11/07/2026** : la barre de navigation basse (Langue / Guide / Modules / Infos) reste masquée au tout premier chargement de l'app tant qu'aucun parcours n'a jamais été commencé (`_isBrandNewUser()` — aucune progression ni guide vu, dans aucun des deux modes). Elle apparaît dès la première interaction (choix d'une langue) et reste ensuite affichée normalement à chaque lancement suivant.

**Ajouté le 11/07/2026 (suite)** : une carte « 🚀 L'essentiel en 30 secondes » (`.hg-tldr`) a été insérée dans l'écran Guide (`#home`), entre l'en-tête coloré (`.home-guide-header`) et la barre sticky Commencer/PDF (`.home-start-sticky`) — donc au-dessus des 8 rubriques en accordéon, visible d'emblée sans rien déplier. Deux versions statiques (FR/ES) dans `index.html`, basculées par le même mécanisme générique que les rubriques (`.home-lang-block[data-lang]` + `_buildHomeGuide()` dans `app.js`) — aucun JS supplémentaire nécessaire. Contenu : 2 niveaux/tabs, spécificité audio propre à chaque sens d'apprentissage (repli de voix régionale espagnole côté FR, repli de voix système côté ES), limite hors-ligne de 🎤 Répète, et recommandation navigateur condensée (détail complet renvoyé vers les rubriques existantes en dessous).

### Chargement conditionnel des données
`loadDataForMode()` injecte dynamiquement `data-fr.js` **ou** `data-es.js` uniquement au moment du choix de langue — réduction de ~50 % du JS parsé au démarrage. Guard contre les doubles appels (`_loadDataInProgress`).

### Système de thèmes CSS
- `html.theme-french` → palette bleu/blanc/rouge (🇫🇷)
- `html.theme-spain` → palette rouge/or (🇪🇸)
- 7 variantes régionales via classes additionnelles (`region-MX`, `region-AR`, `region-CO`, `region-PE`, `region-VE`, `region-EC`, `region-ES`)

---

## 4. Fonctionnalités techniques

### 4.1 Synthèse vocale (Web Speech API — SpeechSynthesis)
- Cascade de sélection de voix espagnole par correspondance régionale (voix exacte de la région → voix espagnole générique disponible en secours → voix système par défaut en dernier recours) — `_resolveSpanishVoice()`. Note : il n'existe pas de distinction voix distante/locale dans le code ; `speechSynthesis.getVoices()` ne renseigne pas cette information de façon fiable inter-navigateurs, la cascade réelle porte uniquement sur la correspondance de langue/région.
- 3 vitesses de lecture paramétrables
- Keep-alive timer pour iOS/Android (évite la coupure silencieuse)
- Badge visuel de qualité de voix (`_updateVoiceBadge()`) — ✅ exacte / ⚠️ secours / ❓ défaut
- Toast et écran d'avertissement si audio indisponible
- **Ajouté le 10/07/2026** : bannière hors-ligne proactive et persistante (`_updateOfflineAudioBanner()`), affichée dès la détection du mode hors ligne (sans attendre un clic) — prévient que la lecture peut basculer sur la langue système par défaut si aucune voix locale correspondante n'est installée
- **Corrigé le 11/07/2026** : la rubrique statique « Hors ligne » du Guide utilisateur (`index.html`) affirmait sans nuance que l'écoute 🔊 fonctionnait hors ligne « entièrement » — désormais alignée sur la nuance déjà présente dans la bannière proactive ci-dessus et dans `_micOfflineHtml()` (repli possible sur la langue système par défaut)


### 4.2 Reconnaissance vocale (Web Speech API — SpeechRecognition)
- Algorithme de Levenshtein pour tolérance aux fautes de prononciation (`_levenshtein()` + `_speechMatch()`)
- Normalisation multilingue des textes reconnus (`_normalizeSpeech()`)
- Feedback visuel vert/orange selon le score de similarité
- Gestion des erreurs microphone bloqué
- Détection hors ligne (`_isOffline()`) : service cloud, indisponible sans connexion sur tous les moteurs (Chrome/Edge/Safari) — signalé au clic (`_micOfflineHtml()`) **et** de façon proactive via la bannière hors-ligne commune décrite en 4.1

### 4.3 Système de progression
- `localStorage` avec clé unique par mode (`STORAGE_KEY`)
- Étoiles 1★ (≥50%) / 2★★ (≥75%) / 3★★★ (100%) — jamais régressives
- Reprise de quiz interrompus via `sessionStorage` (`_saveQuizSession` / `_restoreQuizSession`)
- Barre de progression globale + cercle SVG animé sur la home
- **Ajouté le 12/07/2026** : suivi persistant des modules déjà ouverts au moins une fois
  (`OPENED_STORAGE_KEY`, clé `localStorage` distincte par mode, tableau d'ids), posé au tout
  premier `openTheme()` via `markThemeOpened()`. Distinct du système d'étoiles : un module à
  0 étoile peut avoir été ouvert et raté, ou n'avoir jamais été ouvert — seule cette nouvelle
  clé permet de distinguer les deux cas. `isThemeOpened()` couvre aussi, sans migration
  explicite, les modules déjà étoilés avant l'introduction de ce suivi (avoir des étoiles
  implique forcément d'avoir déjà ouvert le module).
- **Ajouté le 12/07/2026 (suite)** : chaque carte de module affiche désormais l'un des 3 états
  visuels calculés par `getModuleState()` — `state-new` (fond neutre, badge « 🆕 Nouveau »),
  `state-progress` (fond ambre léger, module ouvert mais pas encore à 100 %) ou
  `state-complete` (fond teinté + bordure + coche ✓, réservé aux 3 étoiles — remplace l'ancienne
  classe `.done` qui s'appliquait dès 1 étoile). Étoiles pleines/vides distinguées par classes
  CSS dédiées (`.star-filled` opacité 1, `.star-empty` opacité réduite) plutôt qu'un simple ☆
  non stylé. Le header de la grille affiche en plus une pastille « ✅ X / 48 terminés »
  (modules à 3 étoiles) à côté du compteur d'étoiles existant, distincte du `%` global qui
  compte lui tout module ne serait-ce qu'entamé (≥ 50 %, donc ≥ 1 étoile). Un `resetTheme()`
  individuel ne retire pas le module de `openedThemes` (l'utilisateur le connaît déjà, il n'est
  pas « nouveau ») ; un reset complet (`confirmReset()`) l'efface en revanche intégralement.

### 4.4 Service Worker (stratégie hybride)
- **Cache-First** pour les ressources locales (HTML, CSS, JS, PNG, icônes)
- **Network-First** pour les ressources externes (GitHub Raw, Twemoji CDN)
- 4 fallbacks SVG inline en cas d'échec total (image locale, icône PWA, image externe, page offline)
- Versionnage automatique via `__BUILD_NUMBER__` injecté par GitHub Actions

### 4.5 Export PDF
- Génération en mémoire via `window.print()` dans une fenêtre popup stylée
- 3 types exportables : guide utilisateur, module vocabulaire, dialogue situationnel
- Styles dédiés (`_pdfTheme()`, `_pdfBaseStyles()`, `_pdfHeader()`, `_pdfFooter()`)

### 4.6 Quiz dynamique
- Génération aléatoire à chaque partie — jamais le même quiz (`_generateLevel1Quiz()`)
- QCM 10 questions avec distracteurs intelligents
- Anti double-clic (flags `q10Answered`, `dqAnswered`)
- Confettis animés GPU à 3★★★ (`_launchConfetti()`)

---

## 5. Accessibilité & sécurité

| Aspect | Implémentation |
|---|---|
| **WCAG 1.4.4** | `maximum-scale=5.0` — zoom utilisateur autorisé |
| **Navigation clavier** | `role="button"`, `aria-label` sur tous les éléments interactifs |
| **CSP** | `Content-Security-Policy` via meta tag (GitHub Pages sans headers HTTP) |
| **Clickjacking** | `X-Frame-Options: SAMEORIGIN` |
| **MIME sniffing** | `X-Content-Type-Options: nosniff` |
| **Anti-spam email** | Adresse encodée en reverse dans le HTML |
| **Retour haptique** | `navigator.vibrate()` — feedback discret sur mobile |

---

## 6. Points forts techniques

- ✅ **Zéro dépendance** — pas de npm, pas de bundler, pas de framework
- ✅ **PWA complète** — installable, hors-ligne, 11 icônes, manifest valide
- ✅ **Thèmes CSS par variables** — basculement instantané sans rechargement
- ✅ **Comments de code exhaustifs** — chaque section, fonction, et décision architecturale est documentée
- ✅ **Guard patterns** robustes — double-clic, chargement en cours, voix non disponibles
- ✅ **CI/CD automatisé** — aucune action manuelle au déploiement

## 7. Points d'amélioration potentiels

- ⚠️ `unsafe-inline` en CSP (nécessaire pour le style inline dynamique, mais réduit la protection XSS)
- ⚠️ Variables globales JS nombreuses — une refonte module ES6 améliorerait l'encapsulation
- ⚠️ Pas de tests automatisés (unitaires ou E2E)
- ⚠️ `data-fr.js` et `data-es.js` en JS global (`ALL_THEMES_FR`) — un format JSON + `fetch()` serait plus propre
- ⚠️ Firefox mobile ne supporte pas `SpeechRecognition` (documenté dans le guide, non bloquant)

---

## 8. Historique

| Date | Contenu |
|---|---|
| 07/06/2026 → 29/06/2026 | Version bêta créée avec IA Claude Sonnet 4.6 et Gemini 3.5 Flash |
| 29/06/2026 → 08/07/2026 | Tests : desktop (auto) et iPhone (manuel) par Fédérico ; Android (manuel) par Sébastien Godet, aidé par Gemini 3.5 Flash Extended. Correctifs par Sébastien Godet, aidé par Claude Sonnet 5 |
| 08/07/2026 | Vérification technique (Claude Sonnet 5) : les correctifs ci-dessus avaient ajouté ~103 lignes à `app.js` et ~5 lignes à `style.css` sans que les tables des matières internes (« PLAN DU FICHIER ») ni les métriques de ce document ne soient mises à jour. Recalcul de tous les numéros de ligne dans les en-têtes de `app.js` et `style.css` ; correction de deux erreurs de contenu (en-têtes `data-fr.js`/`data-es.js` indiquant 28 thèmes au lieu de 32) ; ajout d'une entrée manquante dans le plan de `style.css` (mise en page 2 colonnes desktop de l'écran Leçon, ajoutée pendant les correctifs) ; mise à jour des comptes de lignes/poids/fonctions ci-dessus et dans le README |
| 10/07/2026 | Ajout (Claude Sonnet 5, demande utilisateur) d'une bannière hors-ligne proactive et persistante dans l'écran Leçon (`_updateOfflineAudioBanner()`, `#offline-audio-banner`), affichée dès le passage hors ligne sans attendre un clic sur Prononcer/Écouter, et mise à jour en direct via les évènements `online`/`offline`. Nuance apportée au message réactif existant (`_micOfflineHtml()`), qui affirmait sans nuance qu'« Écouter reste disponible hors ligne » — imprécis, car la synthèse vocale peut retomber sur la voix par défaut du système (langue non désirée) si aucune voix locale correspondante n'est installée. Correction du §4.1 ci-dessus, qui décrivait une cascade de sélection de voix « remote > local enhanced > local standard » : cette distinction n'existe pas dans le code (`_resolveSpanishVoice()` ne sélectionne que par correspondance de langue/région, sans notion de voix distante/locale). Recalcul intégral des métriques ci-dessus (§1 et §2) après vérification directe des fichiers (`wc -l` / taille sur disque) : au-delà de l'impact des ~97 lignes ajoutées à `app.js` et ~36 lignes à `style.css` par cette bannière, les comptes de lignes d'`app.js` (4 864 → en réalité déjà 5 033 avant cet ajout) et d'`index.html` (1 049 → en réalité déjà 1 090, fichier non modifié aujourd'hui) se sont révélés sous-évalués indépendamment des correctifs du jour — écart antérieur non expliqué, corrigé ici par mesure directe plutôt que par recalcul déclaratif |
| 10/07/2026 (suite) | Ajout (Claude Sonnet 5, demande utilisateur) d'un encadré comparatif « 🌐 Quel navigateur choisir ? » dans la rubrique 3 « L'audio » du guide utilisateur intégré (`#guideContentFR` et `#guideContentES` dans `index.html`), présentant par plateforme (Android / iPhone-iPad / Ordinateur) le navigateur recommandé et les limites des autres — notamment Firefox, qui ne supporte pas ou mal la reconnaissance vocale (onglet 🎤 Répète) selon la plateforme. Reprend les constats déjà établis dans le README (tableau de compatibilité) et dans les commentaires internes d'`app.js`/`index.html` (ex. `beforeinstallprompt` non supporté par Firefox ni iOS Safari), sans en modifier le contenu factuel. Aucune nouvelle rubrique créée (le guide reste à 8 sections par langue) ; contenu ajouté au sein de la rubrique existante. `index.html` : 1 090 → 1 152 lignes (+62), 67 Ko → 71 Ko. Métriques §1 et §2 recalculées en conséquence |
| 10/07/2026 (suite 2) | Extension (Claude Sonnet 5, demande utilisateur) de l'encadré « 🌐 Quel navigateur choisir ? » ajouté ci-dessus : ajout explicite de Brave, Opera et DuckDuckGo (et mention explicite de Samsung Internet, auparavant seulement citée entre parenthèses) dans les trois colonnes Android / iPhone-iPad / Ordinateur, avec une recommandation propre à chacun. Contenu vérifié le jour même par recherche web (parts de marché navigateurs Statcounter France/Espagne/Mexique/Argentine, mai-juin 2026 : Chrome, Safari, Edge, Firefox, Samsung Internet et Opera présents dans le top 6 de chaque pays ; Brave présent au Mexique ~1,15 % — aucun autre navigateur testé ne ressort significativement dans ces marchés). Brave et Opera classés ✅ (moteur Chromium, même famille que Chrome/Edge — l'appli gère déjà nativement le léger délai de chargement des voix propre à Brave/Android, cf. `_waitForVoicesReady()` §3b). DuckDuckGo classé ❓ (moteur Blink sur Android/Windows, WebKit sur iOS/macOS d'après vérification externe, mais blocage de trackers pouvant perturber l'appel réseau de 🎤 Répète — non vérifié en conditions réelles faute d'accès à l'app, formulé avec prudence dans le guide). `index.html` : 1 152 → 1 146 lignes (texte reformulé en listes plus denses ; taille en octets néanmoins en légère hausse malgré la baisse du nombre de lignes : 71 Ko → 72 Ko). Métriques §1 et §2 recalculées en conséquence |
| 10/07/2026 (suite 3) | Ajout (Claude Sonnet 5, demande utilisateur, après confirmation que la liste soit exhaustive au niveau mondial) de **Vivaldi** et **UC Browser** dans l'encadré « 🌐 Quel navigateur choisir ? », complétant ainsi le top 10 mondial des navigateurs (StatCounter) : Chrome, Safari, Edge, Firefox, Opera, Samsung Internet, UC Browser, Brave, Vivaldi, DuckDuckGo sont désormais tous couverts. Vivaldi classé ✅ sur les trois colonnes (Android/iPhone-iPad/Ordinateur) : moteur Chromium sur Android/Windows/Mac/Linux, WebKit sur iOS comme tout navigateur iOS — appli iOS confirmée disponible et activement mise à jour (vérifié via App Store/communiqués Vivaldi, dernière version 7.9 fin juin 2026). UC Browser classé ⚠️, colonne Android uniquement (usage iOS/desktop marginal) : basé sur Chromium depuis son cœur « U4 », mais son « Mode rapide » historique compresse les pages via un serveur proxy, ce qui peut altérer l'audio et 🎤 Répète — recommandation de le désactiver ou de préférer Chrome, formulée par prudence technique plutôt que par test direct. Note de contexte donnée à l'utilisateur (hors guide, dans la conversation) : ni Vivaldi (~0,4 % mondial) ni UC Browser (~1,1 % mondial, quasi inexistant en France/Espagne/Amérique hispanophone d'après les données pays déjà vérifiées) ne sont significatifs pour le public réel de l'appli — ajoutés pour exhaustivité sur demande explicite. `index.html` : 1 146 → 1 148 lignes (+2), 72 Ko → 72,5 Ko. Métriques §1 et §2 recalculées en conséquence — la liste des navigateurs couverts par le guide est désormais considérée complète |
| 10/07/2026 (suite 4) | Relecture complète (Claude Sonnet 5, demande utilisateur) de l'ensemble des fichiers pour corriger les commentaires internes et leurs numéros de ligne suite aux mises à jour ci-dessus. Constat : le « PLAN DU FICHIER » de `app.js` (recalculé le 08/07) n'avait pas suivi l'ajout de la bannière hors-ligne (~97 lignes vers la ligne 1300) — tous les numéros après ce point étaient décalés d'environ -100 à -200 lignes ; celui de `style.css` était erroné indépendamment de tout ajout récent (écarts de +30 à +90 lignes déjà présents le 08/07, non détectés à l'époque) et son ordre d'affichage ne suivait pas l'ordre réel du fichier. Les deux plans ont été intégralement recalculés par mesure directe (chaque ancre vérifiée une à une par grep, pas par estimation) ; celui de `style.css` a en plus été réordonné pour suivre l'ordre réel des sections dans le fichier, et une entrée « Bannière hors-ligne » y a été ajoutée. Découverte additionnelle : une section entière d'`app.js` — §21c, bouton d'installation PWA native (`_initInstallButtons()`, `_installPwa()`, ajoutée le 09/07/2026) — était absente du plan depuis sa création ; elle y figure désormais. Le compte de fonctions nommées d'`app.js` (156 dans ce document) était également sous-évalué : recompté par grep, il est en réalité de 161 (comptage incluant les fonctions imbriquées nommées, ex. `refresh()`, `_doPrint()`) ; corrigé ci-dessus (§2) et dans le README. `README.md` mis à jour (161 fonctions / 5 130 lignes au lieu de 153 / 4 864 ; ligne d'historique condensée ajoutée pour le 09-10/07). Aucun fichier de données (`data-fr.js`, `data-es.js`), ni `index.html`, ni `sw.js` ne contient de numéro de ligne en commentaire — rien à corriger de ce côté. Aucune ligne de code fonctionnel modifiée dans cette passe, uniquement des commentaires et de la documentation |
| 11/07/2026 | Trois demandes utilisateur traitées par Claude Sonnet 5. **(1)** Barre de navigation basse masquée au tout premier lancement de l'app, tant qu'aucun parcours n'a jamais été commencé dans aucun des deux modes : nouvelle fonction `_isBrandNewUser()` (aucune progression ni guide vu en localStorage → nouvelle entrée §15c dans le PLAN de `app.js`), utilisée pour conditionner l'appel à `_updateBottomNav()` dans le listener `DOMContentLoaded`, qui affichait auparavant la barre inconditionnellement dès le chargement. La barre réapparaît dès la première interaction (clic sur une carte de langue, déjà géré par `showLauncherVariant()`) et, sur les lancements suivants, dès qu'une trace de progression ou de guide vu existe. **(2)** Réordonnancement des 8 rubriques du Guide utilisateur (`#guideContentFR`/`#guideContentES` dans `index.html`), identique dans les deux langues : Comment ça marche → Points forts → L'audio → Réinitialiser un module → Hors ligne → Comparaison → Remerciements → À propos devient Comment ça marche → L'audio → Hors ligne → Réinitialiser un module → Points forts → Comparaison → Remerciements → À propos — regroupe d'abord l'orientation, puis les rubriques pratiques/dépannage (audio, hors ligne, réinitialisation), avant les rubriques de mise en avant (points forts, comparaison) et de clôture (remerciements, à propos). Contenu de chaque rubrique inchangé ; seuls l'ordre d'affichage et la numérotation des commentaires `<!-- N. Titre -->` ont été mis à jour. Le bandeau régional `#guideRegionBadgeES` (hors rubrique, injecté par `_refreshGuideRegion()`) reste bien positionné avant la première rubrique du bloc espagnol — vérifié explicitement après réordonnancement pour ne pas le perdre. **(3)** Nuance ajoutée dans la rubrique Hors ligne / Sin conexión elle-même, jusqu'ici non couverte par la correction du 10/07/2026 (qui ne portait que sur `_micOfflineHtml()` et la nouvelle bannière proactive de leçon) : la phrase affirmant que l'app fonctionne « entièrement hors ligne, écoute 🔊 [...] compris » a été assouplie (suppression de « entièrement »/« completamente »), et un nouvel encadré `.ob-tip` précise que l'écoute peut basculer sur la langue par défaut de l'appareil si aucune voix locale correspondante n'est installée, avec renvoi vers la rubrique L'audio et mention de la bannière proactive équivalente. `app.js` : 5 130 → 5 176 lignes (+46, dont la fonction `_isBrandNewUser()`, sa condition d'appel, et le recalcul du PLAN DU FICHIER — tous les numéros à partir de §16 décalés de +41), 235 Ko → 238 Ko, 161 → 162 fonctions nommées (recompté par grep). `index.html` : 1 148 → 1 150 lignes (+2, les deux nouveaux `.ob-tip`), 72,5 Ko → 73,4 Ko — nombre de rubriques inchangé (8 par langue, seul l'ordre change). Métriques §1 et §2 ci-dessus recalculées en conséquence par mesure directe (`wc -l` / taille sur disque). `style.css`, `data-fr.js`, `data-es.js`, `sw.js`, `manifest.json`, `deploy.yml` non modifiés |
| 11/07/2026 (suite) | Ajout (Claude Sonnet 5, demande utilisateur, inspiré d'une capture d'écran de l'app Oromo du même auteur) d'une carte résumé « 🚀 L'essentiel en 30 secondes » / « 🚀 Lo esencial en 30 segundos » (`.hg-tldr`) en tête de l'écran Guide (`#home`), visible d'emblée sans déplier aucun accordéon. Placée dans `index.html` entre `.home-guide-header` et `.home-start-sticky` (donc avant les 8 rubriques, et le bouton « Commencer » reste juste après la carte comme avant — sa position n'a pas changé). Deux blocs statiques FR/ES (`.home-lang-block[data-lang="fr"\|"es"]`), basculés automatiquement par le mécanisme déjà existant (`_buildHomeGuide()` dans `app.js`, aucune fonction JS ajoutée). Contrairement à l'app Oromo (9 rubriques, dont une rubrique dédiée « Quel navigateur choisir ? » séparée), VACHÉBO n'a que 8 rubriques (la comparaison navigateurs est fusionnée dans la rubrique L'audio depuis le 10/07/2026) — la nouvelle carte reste donc au-dessus de 8 rubriques, pas 9. Contenu adapté aux spécificités réelles de l'app plutôt que recopié tel quel : le repli de voix (bullet 2) mentionne, côté bloc FR, le repli entre les 7 variantes régionales d'espagnol (`_resolveSpanishVoice()`, priorité 2 — n'importe quelle voix espagnole si celle de la région exacte est absente) ; côté bloc ES, le repli plus générique sur la voix système par défaut si aucune voix française locale n'est installée (même logique que l'encadré `.ob-tip` de la rubrique Hors ligne, ajouté le 10/07/2026). Le bullet navigateur reprend, de façon condensée, les recommandations déjà détaillées dans la rubrique L'audio (Chromium ✅, Firefox jamais 🎤 Répète, Safari seul navigateur validé sur iOS), avec renvoi explicite vers le détail ci-dessous ou le bouton ❓ de la barre de navigation basse. `style.css` : nouvelle classe `.hg-tldr` (couleurs fixes vert succès, cohérentes avec `.mic-feedback--ok` déjà utilisée ailleurs dans le fichier — aucune nouvelle variable CSS créée, le compte de 44 variables/161 déclarations est inchangé) et son override de marge desktop dans le `@media (min-width: 768px)` existant ; 4 498 → 4 556 lignes (+58), 148 Ko → 150 Ko. PLAN DU FICHIER de `style.css` intégralement recalculé, chaque ancre revérifiée une à une par grep (pas par estimation) suite au décalage. `index.html` : 1 150 → 1 174 lignes (+24), 73,4 Ko → 75,7 Ko ; aucun numéro de ligne en commentaire dans ce fichier, rien à recalculer de ce côté. Métriques §1 et §2 ci-dessus recalculées en conséquence par mesure directe (`wc -l` / taille sur disque). `app.js`, `data-fr.js`, `data-es.js`, `sw.js`, `manifest.json`, `deploy.yml` non modifiés |
| 12/07/2026 | 4 améliorations visuelles (Claude Sonnet 5, demande utilisateur, inspirées d'une capture d'écran de l'app Oromo du même auteur) sur les cartes de module de l'écran Sections. **(1)** Fond de carte teinté selon l'état de progression, calculé par la nouvelle fonction `getModuleState()` : `state-new` (fond neutre, inchangé), `state-progress` (fond ambre léger, nouvelles variables `--c-state-progress-bg`/`-border`, volontairement indépendantes du thème pour rester reconnaissables en FR comme en ES), `state-complete` (fond teinté dérivé de `--c-done` + bordure + coche ✓ — remplace l'ancienne classe `.done` qui s'appliquait dès 1 étoile ; réservée désormais aux 3 étoiles/100 %). **(2)** Badge « 🆕 Nouveau » sur les modules jamais ouverts : nouveau suivi persistant `openedThemes`/`OPENED_STORAGE_KEY` (clé `localStorage` distincte par mode, sur le modèle de `STORAGE_KEY`/`done`), posé au tout premier `openTheme()` via `markThemeOpened()` — distinct des étoiles pour ne pas confondre « jamais essayé » et « essayé mais raté » (0 étoile dans les deux cas). `isThemeOpened()` traite aussi comme « ouvert » tout module déjà étoilé avant l'introduction de ce suivi, sans migration explicite nécessaire. **(3)** Étoiles plus contrastées : `.star-filled` (pleines, opacité 1) et `.star-empty` (vides, opacité réduite) remplacent le simple caractère ☆ non stylé. **(4)** Pastille « ✅ X / 48 terminés » (modules à 3 étoiles) ajoutée à côté du compteur d'étoiles existant en tête de la grille, distincte du `%` global qui compte lui tout module ne serait-ce qu'entamé (≥ 50 %, donc ≥ 1 étoile) — `.stars-pill`/`.completed-pill`, fond translucide blanc cohérent avec les pastilles déjà utilisées ailleurs dans l'app (ex. `.launcher-region-btn`). Un `resetTheme()` individuel ne retire pas le module de `openedThemes` (l'utilisateur le connaît déjà) ; un reset complet (`confirmReset()`) l'efface intégralement, y compris sa clé `localStorage` dédiée. `app.js` : 5 176 → 5 266 lignes (+90, dont 5 nouvelles fonctions nommées : `loadOpened()`, `saveOpened()`, `markThemeOpened()`, `isThemeOpened()`, `getModuleState()`), 238 → 249 Ko, 162 → 167 fonctions nommées. `style.css` : 4 556 → 4 638 lignes (+82), 150 → 157 Ko, 44 → 48 variables CSS uniques (161 → 171 déclarations). PLAN DU FICHIER de `app.js` et de `style.css` intégralement recalculés, chaque ancre revérifiée une à une par grep (pas par estimation) — méthode identique à celle du 08/07 et du 10/07 (suite 4). Métriques §1 et §2 ci-dessus recalculées en conséquence par mesure directe (`wc -l` / taille sur disque). `index.html`, `data-fr.js`, `data-es.js`, `sw.js`, `manifest.json`, `deploy.yml` non modifiés |
| 12/07/2026 (suite) | Deux ajouts (Claude Sonnet 5, demande utilisateur) restés non documentés jusqu'à la relecture du 13/07 ci-dessous. **(1)** Le bouton natif d'installation PWA (`.pwa-install-btn`, géré par `_initInstallButtons()`/`_installPwa()`, §21c d'`app.js`) est dupliqué en tête de l'écran Guide (`#homeInstallBtn`, nouvelle classe `.home-install-btn` dans `style.css`) pour le rendre visible dès l'arrivée sur l'écran, à l'inspiration de l'app Oromo du même auteur ; il est en contrepartie retiré de son ancien emplacement dans la rubrique « Hors ligne » du Guide, où il faisait doublon avec les instructions manuelles restées en place. Son libellé bilingue est mis à jour dans `_buildHomeGuide()` (§17), au même endroit que les autres libellés dynamiques de l'écran ; son affichage conditionnel (uniquement si `beforeinstallprompt` est proposé par le navigateur) reste géré par `_initInstallButtons()`, inchangé sur le fond. **(2)** Vérification proactive des mises à jour du Service Worker, ajoutée dans le script d'enregistrement d'`index.html` : `reg.update()` est désormais appelé à chaque retour au premier plan (`visibilitychange`) et toutes les 60 minutes tant que l'app reste ouverte, en complément de la vérification unique déjà faite par `register()` au chargement de la page — utile pour les sessions longues (app installée gardée ouverte plusieurs jours). Aucun effet hors-ligne (l'appel échoue silencieusement si hors connexion). `index.html` : 1 174 → 1 229 lignes (+55), 75,7 → 80,6 Ko. `app.js` : 5 266 → 5 289 lignes (+23, câblage du libellé de `#homeInstallBtn` dans `_buildHomeGuide()` et commentaires de `_initInstallButtons()`), 249 → 249,9 Ko, 167 fonctions nommées (inchangé, aucune nouvelle fonction). `style.css` : 4 638 → 4 667 lignes (+29, nouvelle classe `.home-install-btn`), 157 → 158,4 Ko, 48 variables CSS / 171 déclarations (inchangé). `data-fr.js`, `data-es.js`, `sw.js`, `manifest.json`, `deploy.yml` non modifiés |
| 13/07/2026 | Relecture complète (Claude Sonnet 5, demande utilisateur) pour recaler les commentaires internes suite aux ajouts du 12/07 (suite) ci-dessus, restés hors du « PLAN DU FICHIER » d'`app.js` et de `style.css` au moment de leur ajout. Les deux plans ont été intégralement recalculés par mesure directe (chaque ancre vérifiée une à une par grep, pas par estimation), avec deux nouvelles entrées : le bouton `.home-install-btn` (§6, sous-entrée dans `style.css`) et son câblage dans `_buildHomeGuide()` (référencé dans l'entrée §17 d'`app.js`). Correction additionnelle : plusieurs commentaires liés aux états visuels de carte / badge « 🆕 Nouveau » portaient encore la date du 11/07/2026 alors que cette fonctionnalité a été ajoutée le 12/07/2026 (cf. entrée ci-dessus) — daté erroné, corrigé dans `app.js` et `style.css` (3 occurrences dans chaque fichier). Une référence de ligne obsolète dans `style.css` (§24b, commentaire renvoyant vers « lignes 2264 et 2427 » pour les blocs de couleur d'accentuation en mode sombre — numéros hérités d'une version bien antérieure du fichier, jamais mis à jour depuis) a été corrigée vers les lignes réelles actuelles. `app.js` : 5 289 → 5 295 lignes (+6, dû au recalcul du PLAN DU FICHIER lui-même, qui occupe légèrement plus de place que l'ancienne version). `style.css` : 4 667 → 4 671 lignes (+1, dû à la reformulation de la référence de lignes ci-dessus). `README.md` mis à jour (nombres de lignes d'`app.js`/`style.css`, et deux lignes d'historique ajoutées pour couvrir le 12/07 (suite) et cette relecture). Aucune ligne de code fonctionnel modifiée dans cette passe, uniquement des commentaires et de la documentation. Métriques §1 et §2 ci-dessus recalculées en conséquence par mesure directe (`wc -l` / taille sur disque) |
| 19/07/2026 | ⚠️ **Note de transparence (Claude Sonnet 5).** En reprenant le projet ce jour, plusieurs modifications déjà présentes dans les fichiers fournis (dont le système de mascotte régionale `REGION_MASCOTS`, confirmé par l'utilisateur comme fait le jour même) ne figuraient dans aucune ligne de cet historique ni du `README.md`, qui s'arrêtaient tous deux au 13/07/2026. Écart mesuré par comparaison directe avec les métriques du 13/07 ci-dessus : `app.js` +212 lignes (5 295 → 5 507 avant la présente session), `style.css` +395 lignes (4 671 → 5 066), `index.html` +420 lignes (1 229 → 1 649). Plusieurs commentaires internes datés du 19/07/2026 confirment d'autres changements non couverts par cette note (ex. bouton « Quitter » du lanceur, réorganisation du Guide en 3 zones, correctif de contraste), en plus du système de mascotte. Cette entrée ne reconstitue PAS le détail de ces changements (hors périmètre de la présente session, et risque d'inexactitude en reconstruisant a posteriori plutôt qu'en mesurant au moment du changement, contrairement à la méthode habituelle de ce document) — elle vise seulement à signaler l'écart pour que l'utilisateur puisse, s'il le souhaite, faire journaliser ce lot séparément. Voir l'entrée suivante pour le détail (mesuré directement, lui) des ajouts de la présente session |
| 19/07/2026 (suite) | Extension de la mascotte régionale (Claude Sonnet 5, demande utilisateur : « customiser davantage, mettre un peu d'humour comme le logo, des références un peu partout, spécifiques à chaque variante, en conservant les couleurs des drapeaux mais en les mixant »), en s'appuyant sur `REGION_MASCOTS` déjà en place (voir note ci-dessus). **(1) Couleurs mixées :** trois nouvelles variables partagées `--c-vache-cream` (#FCFFEE, le crème du fond du logo — identique à `background_color` de `manifest.json`) et `--c-vache-brown` / `--c-vache-brown-soft` (#6B4226, le brun du pelage), déclarées une seule fois au `:root` et donc communes aux 7 variantes + thème FR (non redéfinies par région, sur le même principe que `--c-state-progress-bg`). Le point médian blanc/blanc cassé de `--c-grad-home` est remplacé par ce crème pour le thème FR, la base ES, le Mexique, le Pérou et l'Argentine (5 blocs) ; pour l'Équateur, la Colombie et le Venezuela (tricolores sans point neutre), un souffle de brun est inséré entre les deux dernières couleurs plutôt qu'un point médian complet, pour ne pas dénaturer leur tricolore. Un halo radial brun supplémentaire et discret est ajouté au fond de l'écran `#home`, commun à tous les thèmes (`background-blend-mode: overlay` existant, un radial-gradient de plus). Couleurs primaires/accent de chaque région inchangées — seule la teinte de fond est mixée. **(2) Nouveaux points d'humour :** sur un sans-faute (3 étoiles ⭐⭐⭐), l'écran de résultat des deux quiz (`renderQuiz10()` et `renderDialogQuiz()`, via `_quizResultStrings()`) affiche désormais une ligne mascotte (nouveau champ `mascotLine` retourné par la fonction) — l'exclamation régionale `REGION_MASCOTS[currentRegion]` en mode Espagnol, un jeu de mots français (« vachement fort·e ») en mode Français, faute de région à distinguer côté FR. Nouvelle classe `.quiz-mascot-line` (`style.css`). Nouvelle fonction `_refreshFooterMascot()` qui signe les deux pieds de page (`#footerMascotLauncher` dans le Lanceur, `#footerMascotLesson` dans l'écran leçon — nouveaux `<span>` dans `index.html`) d'un `🐄` + symbole + exclamation régionale (ES) ou clin d'œil au slogan du logo « Oh la vache, qué chévère ! » (FR) ; appelée par `pickRegion()` à chaque changement de variante et par `initApp()` au premier affichage. Nouvelle classe `.footer-mascot`, avec une surcharge de couleur en mode sombre (`#D9A066` au lieu de `--c-vache-brown`, insuffisant en contraste sur fond `#14172a`). **Vérifications :** `node --check app.js` sans erreur ; accolades CSS comptées équilibrées (912/912) ; balises `<footer>` comptées équilibrées (4/4). `app.js` : 5 507 → 5 546 lignes (+39, dont 1 nouvelle fonction nommée `_refreshFooterMascot()` — 177 → 178 fonctions nommées de premier niveau, recompté par grep), 267,0 → 269,1 Ko. `style.css` : 5 066 → 5 106 lignes (+40), 179,8 → 181,9 Ko, 48 → 51 variables CSS uniques (176 → 179 déclarations). `index.html` : 1 649 → 1 651 lignes (+2, les deux `<span>` de signature). Métriques §1 et §2 ci-dessus recalculées en conséquence par mesure directe (`wc -l` / taille sur disque) — en partant toutefois de l'état du 19/07 déjà non documenté (voir note ci-dessus), pas du 13/07. `data-fr.js`, `data-es.js`, `sw.js`, `manifest.json`, `deploy.yml` non modifiés. PLAN DU FICHIER de `app.js`/`style.css` non recalculé dans cette passe (périmètre : contenu, pas la table des matières) — à faire lors d'une prochaine relecture complète, comme les 10/07 (suite 4) et 13/07 ci-dessus |
| 21/07/2026 (2e passe) | ⚠️ **Note de transparence (Claude Sonnet 5).** Reprise du projet ce jour sur des fichiers déjà porteurs de commentaires datés « 21/07/2026 » non couverts par une entrée d'historique (fusion des icônes de navigation + logo centré dans le Lanceur et les écrans Sections/Modules ; centrage du bandeau variante régionale du Guide). Comme pour la note du 19/07/2026 ci-dessus, cette entrée ne reconstitue pas ce lot antérieur (hors mesure directe, donc risque d'inexactitude) — elle documente uniquement les 2 correctifs demandés par l'utilisateur dans la présente session, qui affinent ce même lot déjà en place. **(1) Logo des écrans Modules encore trop petit** : `.sections-logo-img` (`style.css`) passe de `max-width:190px / width:52%` à `max-width:240px / width:68%` — logo plus visible dans le bandeau du haut des écrans Niveau 1 et Niveau 2, sans changer sa position (toujours centré entre les 2 groupes d'icônes via `.sections-logo-strip` en `flex:1`, inchangé). **(2) Drapeau et élément culturel du bandeau du Guide peu visibles** : le bandeau `#guideRegionBadgeES`/`#guideRegionBadgeFR` (généré par `_refreshGuideRegion()` dans `app.js`) affichait le drapeau et le symbole mascotte (🐄 + emoji régional) à la même taille que le texte descriptif — le centrage du bandeau dans son ensemble (fait lors du lot du 21/07 non détaillé ci-dessus) ne les rendait pas plus visibles pour autant. Le `innerHTML` généré est scindé en 2 lignes : `.ghrb-icons` (drapeau + 🐄 + mascotte, `font-size:2.4rem`, centré) au-dessus de `.ghrb-text` (phrase descriptive, `font-size:.85rem`) — la phrase reste avant la rubrique accordéon « Cómo funciona / Comment ça marche », seule sa présentation change. `.guide-hero-region-badge` ajusté en conséquence (`padding`, `border-radius`, `max-width:92%` pour éviter tout débordement sur petits écrans). **Vérifications :** `node --check app.js` sans erreur ; accolades CSS comptées équilibrées (936/936) ; aucune nouvelle variable CSS ni fonction JS nommée ajoutée (2 nouvelles classes CSS seulement : `.ghrb-icons`, `.ghrb-text`). `app.js` : 5 821 → 5 827 lignes (+6, commentaires inclus), 278 986 → 279 445 octets. `style.css` : 5 303 → 5 322 lignes (+19), 189 721 → 190 606 octets. `index.html`, `data-fr.js`, `data-es.js`, `sw.js`, `manifest.json`, `deploy.yml` non modifiés. Comme pour le lot antérieur du 19/07, les métriques §1/§2 ci-dessus et le README ne sont **pas** recalculés dans cette entrée (portée : uniquement les 2 correctifs ci-dessus) ; le PLAN DU FICHIER de `app.js`/`style.css` reste également non recalculé — le décalage cumulé (lot non documenté du 21/07 + présente passe) devra être traité lors d'une prochaine relecture complète dédiée, comme suggéré le 19/07 |
| 21/07/2026 (3e passe) | Deux correctifs supplémentaires (Claude Sonnet 5, demande utilisateur, capture d'écran à l'appui) sur les 2 points de la passe précédente, pas encore satisfaisants une fois vus en conditions réelles. **(1) Logo des écrans Modules toujours jugé trop petit** : `.sections-logo-img` repoussé une 2e fois, de `max-width:240px / width:68%` à `max-width:260px / width:88%`. Comme le conteneur du logo (`.sections-logo-strip`, `flex:1`) partage la largeur du bandeau avec les 2 groupes de boutons icônes de chaque côté (`flex-shrink:0`, largeur fixe), une hausse de `width` seule restait limitée par cette largeur disponible ; les boutons `.back-icon-btn` de cette ligne précise ont donc aussi été réduits (`40px`→`34px`, `1.3rem`→`1.15rem`), via un nouveau sélecteur scopé `.sections-top-icons-row .back-icon-btn` pour ne pas affecter ce même bouton utilisé ailleurs (Lanceur, bouton 🚪 Quitter, etc., qui gardent leur taille d'origine) ; espacement entre les 2 boutons de chaque groupe resserré (`gap:6px`→`3px`) pour la même raison. **(2) « Texte en arrière-plan » quasi invisible + grand espace vide avant « Cómo funciona »** : la vraie cause, repérée en relisant le CSS plutôt qu'en ajustant à l'aveugle, est que `.guide-hero-region-badge` vit dans `.home-guide-body`, un corps **blanc** (`rgba(255,255,255,.92)`) — son couple fond translucide blanc (`rgba(255,255,255,.22)`) / texte blanc à 95 % (`.ghrb-text`), pensé pour un fond dégradé coloré (cf. commentaire de la 2e passe), y devenait quasi illisible : blanc sur blanc, d'où l'impression de « texte fantôme » et de vide au-dessus de l'accordéon (l'espace occupé par ce texte semblait vide puisqu'il n'y était pour ainsi dire pas visible). Corrigé en réutilisant les variables de thème déjà présentes plus bas dans `style.css` pour ce même bloc (`--g-fr`/`--g-fr-lt` bleu clair pour `#guideContentFR`, `--g-es`/`--g-es-lt` rouge clair pour `#guideContentES` — même principe que les règles existantes `#guideContentES .guide-stat-num { color: var(--g-es); }` etc., aucune nouvelle variable créée) : fond clair teinté + texte foncé bien contrasté, au lieu du blanc sur blanc. Marges resserrées au passage (`margin-bottom` du bandeau 14px→10px) pour que l'ensemble icônes + texte forme un bloc compact juste au-dessus de l'accordéon, sans espace superflu. **Vérifications :** `node --check app.js` sans erreur ; accolades CSS comptées équilibrées (940/940) ; aucune nouvelle variable CSS ni fonction JS nommée ajoutée (réutilisation de variables et sélecteurs existants uniquement, 1 nouveau sélecteur scopé `.sections-top-icons-row .back-icon-btn`). `app.js` non modifié dans cette passe (5 827 lignes, 279 445 octets — inchangé depuis la 2e passe). `style.css` : 5 322 → 5 351 lignes (+29), 190 606 → 192 249 octets. Cumul depuis les fichiers d'origine de cette session : `app.js` 5 821 → 5 827 lignes (+6, 2e passe uniquement), `style.css` 5 303 → 5 351 lignes (+48). `index.html`, `data-fr.js`, `data-es.js`, `sw.js`, `manifest.json`, `deploy.yml` non modifiés. Portée de cette entrée limitée aux 2 correctifs ci-dessus, comme la précédente — métriques §1/§2, README et PLAN DU FICHIER toujours non recalculés (même remarque qu'en 2e passe) |
| 22/07/2026 | ⚠️ **Note de transparence (Claude Sonnet 5).** Reprise du projet ce jour sur des fichiers déjà plus longs que la dernière mesure directe de ce document (21/07/2026, 3e passe) : `app.js` 5 827 → 5 876 lignes (+49), 279 445 → 282 628 octets ; `style.css` 5 351 → 5 423 lignes (+72), 192 249 → 197 316 octets ; `index.html` 1 651 → 1 779 lignes (+128 ; taille en octets non consignée à l'époque pour ce fichier, seulement en Ko au § 2 — delta d'octets donc non calculable ici). Aucune nouvelle variable CSS (51 uniques / 179 déclarations, recompté et inchangé) ni fonction JS nommée de premier niveau (178, recompté et inchangé) dans cet écart : il correspond à du contenu (commentaires, réglages fins) plutôt qu'à de nouvelles fonctionnalités structurelles. Comme pour les notes du 19/07 et du 21/07 (2e passe) ci-dessus, cette entrée ne reconstitue pas le détail de ce lot antérieur (hors mesure directe, donc risque d'inexactitude) — elle signale seulement l'écart. Voir l'entrée suivante pour le détail, mesuré directement, des corrections apportées dans la présente session |
| 22/07/2026 (suite) | Huit correctifs visuels et d'accessibilité (Claude Sonnet 5, demande utilisateur — les 3 premiers avec capture d'écran à l'appui, les autres suite à une relecture demandée puis à 2 nouvelles captures). **(1) Pieds de carte « Sobre/Infos VACHÉBO » désalignés :** `.lang-card` n'avait pas de mise en page flex interne — quand `align-items:stretch` (déjà en place depuis un lot antérieur) étirait la carte la plus courte pour égaler la hauteur de l'autre, l'espace ajouté restait en bas de la boîte plutôt que d'être absorbé au-dessus de `.lang-card-toggle-bar`, laissant les deux barres à des hauteurs différentes selon la longueur du texte FR/ES. Corrigé en passant `.lang-card` en `display:flex; flex-direction:column` et en ajoutant `margin-top:auto` sur `.lang-card-toggle-bar`, qui absorbe désormais tout excédent d'espace et ancre la barre tout en bas des deux cartes, quel que soit leur état (replié/déplié) ou la langue. **(2) Ordre de l'en-tête de l'écran Modules inversé** (demande explicite) : `.sections-top-row` (titre + drapeau) affiché désormais avant `.sections-top-icons-row` (icônes + logo), sur `#sections-level1` et `#sections-level2` ; contenu de chaque bloc inchangé, seul l'ordre des 2 `<div>` est inversé. **(3) Espace vide sous le bandeau régional du Guide :** `.guide-hero-region-badge` portait un `margin-bottom:10px` qui, cumulé au `padding-top:12px` de `.hg-summary` juste en dessous, créait un espace vide perceptible avant le 1er accordéon — `margin-bottom` retiré, le `padding` de `.hg-summary` suffisant seul à séparer les 2 blocs. **(4) Libellé de bouton erroné dans le Guide ES** (repéré lors d'une relecture demandée par l'utilisateur, sans capture) : la rubrique « Reiniciar un módulo » affirmait 2 fois que le bouton de la carte de module s'appelle « Reiniciar », alors que le bouton réel (`_buildThemeCard()`, `app.js`) dit « 🔄 Volver a empezar » — les 2 occurrences corrigées en conséquence. **(5) aria-label du bouton 🔄 de réinitialisation globale rendu dynamique** selon la langue de l'apprenant (et non plus figé en français) : `Reiniciar la progresión` en mode `learn_french` (apprenant hispanophone), `Réinitialiser la progression` en mode `learn_spain` — mis à jour dans `_fillHeader()` (fonction interne de `renderSections()`), qui gère déjà les autres libellés dynamiques du même en-tête. Choix distinct du format bilingue concaténé des icônes voisines (🏠/🌎/❓/🚪, volontairement figé) car l'utilisateur a demandé explicitement un libellé dépendant de la langue de l'apprenant. **(6) z-index des modales de confirmation relevé au-dessus du toast :** `.app-toast` (`z-index:10000`) pouvait visuellement recouvrir une modale de confirmation ouverte (`z-index:9999`) si les 2 se déclenchaient en même temps (cas rare, `.app-toast` a `pointer-events:none` donc aucun clic n'était jamais bloqué, mais le rendu visuel restait trompeur) — les 3 modales concernées (`#confirm-modal`, `#quit-modal`, et `#credits-modal` qui portait le même `z-index:9999` mais avait été omise du 1er passage) relevées à `z-index:10001`. **(7) Espacement Lanceur :** `.launcher-subtitle-row` (icônes + « Choisis ta langue · Elige tu idioma ») n'avait qu'un `margin:0 auto` (centrage horizontal seul, aucun espace vertical) avant `.launcher-cards` juste en dessous, donnant un rendu collé — `margin-bottom:14px` ajouté. **(8) Espacement Guide :** `.hg-tldr` (carte « L'essentiel en 30 secondes ») avait `margin:0 14px 14px` (marge haute nulle), collée au bord bas de `.home-start-sticky` (barre Commencer/PDF) juste au-dessus — marge haute passée à `14px` (et surcharge desktop `@media (min-width:640px)` alignée en conséquence, `0 24px 14px` → `14px 24px 14px`). **Vérifications :** `node --check app.js` sans erreur ; accolades CSS comptées équilibrées (937/937, inchangé — aucun nouveau bloc de règle, uniquement des propriétés modifiées dans des sélecteurs existants) ; 178 fonctions nommées de premier niveau dans `app.js` (inchangé, aucune nouvelle fonction, uniquement un ajout dans `_fillHeader()` déjà existante). `app.js` : 5 876 → 5 892 lignes (+16), 282 628 → 283 495 octets. `style.css` : 5 423 → 5 453 lignes (+30), 197 316 → 199 376 octets. `index.html` : 1 779 → 1 786 lignes (+7), 116 659 → 117 191 octets. `data-fr.js`, `data-es.js`, `sw.js`, `manifest.json`, `deploy.yml` non modifiés. Comme pour les lots des 19/07 et 21/07, les métriques §1/§2 ci-dessus et le PLAN DU FICHIER de `app.js`/`style.css` ne sont **pas** recalculés dans cette entrée (portée : les 8 correctifs ci-dessus uniquement) |
| 23/07/2026 | Ajout dans `app.js` du même historique que celui déjà ajouté la veille dans `README.md` (Claude Sonnet 5, demande utilisateur explicite) : « 18/07/2026 → 23/07/2026 Expériences utilisatrice (Sandrine, Moi) et correctifs avec Claude Sonnet 5. » insérée dans la section `HISTORIQUE DE L'APPLICATION` de l'en-tête de fichier, à la suite de l'entrée « 08/07/2026 → 12/07/2026 » — 2 lignes de contenu ajoutées à cet endroit précis. **Recalcul complet du PLAN DU FICHIER** rendu nécessaire par ce décalage : les 36 ancres `§N L.XXXX` (sections principales + sous-sections a/b/c/d) et les 3 renvois internes embarqués dans le texte descriptif (appel de `_markModulesVisited()` depuis `renderSections()` ; définition de `_mascotSansFauteLine()` ; définitions de `_markModulesVisited()`/`_hasVisitedModules()`) ont été revérifiés un par un — méthode : localisation de chaque bannière ou fonction par son texte exact (`grep`), et non plus par simple report d'un delta de lignes, pour éviter de propager une erreur d'arithmétique. Un script de vérification automatisé a ensuite recontrôlé les 36 ancres + les 3 renvois contre le fichier réellement écrit : tous corrects à la 1ʳᵉ vérification pour les renvois internes, 1 faux-positif détecté et corrigé sur la méthode de vérification elle-même pour §0 (le script cherchait la ligne de la fonction `loadDataForMode()` au lieu de la bannière de section — l'ancre du fichier, elle, était juste). Décalage net observé : le nouvel historique (+2 lignes) et le paragraphe d'explication du recalcul lui-même, plus détaillé que celui du 20/07 (référence explicite au correctif du 22/07 sur `_fillHeader()`, +quelques lignes), donnent un décalage de +7 lignes pour tout le code situé après l'en-tête (mesuré directement sur le fichier réécrit, pas anticipé à l'avance). **Compteurs ARCHITECTURE corrigés au passage** (constatés obsolètes en marge de cette relecture) : `index.html` « 4 écrans, 2 modales » → **5 écrans, 3 modales** (compte réel des éléments `class="screen"` et `class="modal"` du DOM : `app-launcher`, `home`, `sections-level1`, `sections-level2`, `lesson` ; `confirm-modal`, `quit-modal`, `credits-modal` — cette 3ᵉ modale existait déjà avant la présente session, simplement jamais comptée ici) ; `style.css` « 48 variables CSS, 171 décl. » → **51 variables CSS, 179 décl.** (recompte direct, correspond en fait à la valeur déjà documentée au § 2 de ce document depuis le 19/07/2026 — l'en-tête d'`app.js` n'avait simplement jamais été resynchronisé) ; ligne totale d'`app.js` mise à jour dans son propre commentaire ARCHITECTURE. **Vérifications :** `node --check app.js` sans erreur ; 178 fonctions nommées de premier niveau (inchangé) ; script Python de recoupement systématique (36 ancres + 3 renvois) exécuté contre le fichier final, 100 % correct après correction du faux-positif de méthode. `app.js` : 5 892 → 5 899 lignes (+7), 283 495 → 284 015 octets (+520). `style.css`, `index.html`, `data-fr.js`, `data-es.js`, `sw.js`, `manifest.json`, `deploy.yml` non modifiés. `README.md` : ligne de synthèse « 18/07/2026 → 22/07/2026 » étendue à « 18/07/2026 → 23/07/2026 » (le travail utilisateur/correctifs s'étant poursuivi ce jour), et une ligne dédiée ajoutée pour le 23/07/2026 renvoyant à la présente entrée. Portée de cette entrée limitée à l'historique, au PLAN DU FICHIER et aux compteurs ARCHITECTURE de `app.js`, plus la mise à jour de dates du README ; les métriques § 1/§ 2 de ce document restent, comme précédemment, non recalculées dans leur intégralité |
| 26/08/2026 | ⚠️ **Note de transparence (Claude Sonnet 5).** Reprise du projet ce jour sur des fichiers déjà porteurs de commentaires et dates plus récents que la dernière mesure directe de ce document (19/07/2026 pour `app.js`/`style.css`/`index.html` au § 1/§ 2 ; 23/07/2026 pour le PLAN DU FICHIER) : `app.js` réel reçu en session = 5 935 lignes, 286 013 octets ; `index.html` réel = 1 786 lignes, 117 191 octets — cette entrée ne reconstitue pas ces écarts antérieurs (hors mesure directe), elle documente uniquement le travail de la présente session. **Enrichissement du vocabulaire** (demande utilisateur, à partir d'une liste de manques identifiés pour les grands débutants — adjectifs de base, quantité/fréquence/temps, connecteurs/prépositions, ordinaux, lacunes dans Famille/Objets/Santé/Argent). **4 nouvelles rubriques Niveau 1**, ajoutées en miroir dans `data-fr.js`/`data-es.js` à des emplacements choisis pour la progression pédagogique plutôt qu'en fin de liste : **Adjectifs de base / Adjetivos Básicos** (20 mots, 10 paires d'opposés : grande/pequeño, bueno/malo, nuevo/viejo, bonito/feo, fácil/difícil, caro/barato, rápido/lento, alto/bajo, limpio/sucio, abierto/cerrado), insérée après « Les Verbes » et avant « Les Émotions » ; **Mots de Liaison / Conectores** (14 mots : y/o/pero/porque/entonces/con/sin/en/sobre/debajo/dentro/fuera/para/por) et **Quantité, Fréquence et Temps** (17 mots : mucho/poco/demasiado/bastante/siempre/nunca/a veces/a menudo/también/tampoco/ya/todavía/ahora/antes/después/luego), insérées après « Les Couleurs » et avant « La Vie Quotidienne » ; **L'Argent / El Dinero** (10 mots : dinero, tarjeta de crédito/débito, efectivo, cajero automático, cambio, precio, monedas y billetes, recibo, gratis), insérée après « La Santé et la Sécurité » et avant « Le Voyage ». **Rubriques existantes enrichies** (mêmes ajouts dans les 2 fichiers, mots insérés près de l'entrée existante la plus proche sémantiquement plutôt qu'en bloc à la fin) : « Les Nombres » +10 ordinaux (primero → décimo, à la suite des cardinaux) ; « La Famille » +9 mots (el bebé, el niño/la niña, los hijos, el nieto/la nieta, el primo/la prima, el sobrino/la sobrina, soltero/casado/divorciado — ex. « el nieto/la nieta » inséré juste après « el abuelo », pas en fin de liste) ; « Objets du Quotidien » +6 mots numériques (el wifi, Internet, la contraseña, la aplicación, el mensaje, la batería), regroupés avec le matériel high-tech existant (teléfono, móvil, ordenador, cargador) ; « La Santé » renommée **« La Santé et la Sécurité » / « La Salud y la Seguridad »** (id `sante` conservé à l'identique, donc aucun impact sur la progression déjà sauvegardée des utilisateurs) +6 mots de sécurité générale (¡socorro!/¡ayuda!, el accidente, la policía, los bomberos, el robo, el ladrón/la ladrona), ajoutés à la suite de « las urgencias ». **`index.html`** : les 2 mentions du Guide (« Vocabulaire (32 thèmes) » / « Vocabulario (32 temas) ») mises à jour en **36** — seul changement dans ce fichier. **`app.js`** : commentaire ARCHITECTURE (32→36 thèmes) et commentaire de `_renderOtherModeProgress()` (48→52 modules, 32→36 thèmes) mis à jour **sans ajouter ni retirer de ligne**, pour ne pas décaler le PLAN DU FICHIER ni nécessiter son recalcul ; valeur de repli `ALL_THEMES.length || 48` → `|| 52`. **Vérifications :** `node --check` sans erreur sur les 3 fichiers JS modifiés (`app.js`, `data-fr.js`, `data-es.js`) ; comptage programmatique (Node, lecture directe des tableaux `LEVEL1_THEMES_FR`/`_ES`) confirmant 36 thèmes dans l'ordre attendu et un total `ALL_THEMES` de 52 (36+16) dans les deux modes ; contenu des mots vérifié identique entre `data-fr.js` et `data-es.js` (mêmes paires `es`/`fr`, seuls `name`/`sub` du thème diffèrent) pour toutes les rubriques touchées. `data-fr.js` : 1 555 → 1 661 lignes (+106), 116 717 → 121 988 octets (+5 271). `data-es.js` : 931 → 1 035 lignes (+104), 112 677 → 117 880 octets (+5 203). `app.js` : 5 935 → 5 935 lignes (inchangé), 286 013 → 286 034 octets (+21, commentaires et valeur numérique uniquement — aucune fonction ajoutée, PLAN DU FICHIER non impacté). `index.html` : 1 786 → 1 786 lignes (inchangé), 117 191 → 117 191 octets (inchangé, « 32 » → « 36 » à taille de chaîne égale). `style.css`, `sw.js`, `manifest.json`, `deploy.yml` non modifiés. **Portée limitée aux éléments ci-dessus** : les lignes `data-fr.js`/`data-es.js` du tableau § 2 ci-dessus sont mises à jour avec leurs valeurs réelles mesurées cette session (et repartent donc de l'état réel des fichiers reçus — 1 555/931 lignes — différent de la dernière valeur consignée dans ce tableau au 19/07/2026, à savoir 1 530/880 lignes, écart antérieur non couvert par la présente entrée) ; le reste des métriques § 1 (poids total source, chargement initial) et les compteurs ARCHITECTURE `app.js`/`style.css`/`index.html` autres que ceux listés ci-dessus restent, comme lors des sessions précédentes non suivies d'un recalcul complet, dans l'état du 19/07/2026 / 23/07/2026 selon le cas |
| 26/08/2026 (suite) | Séparation (Claude Sonnet 5, demande utilisateur, dans la continuité immédiate de l'entrée précédente) de la rubrique fusionnée « La Santé et la Sécurité » en **2 rubriques Niveau 1 distinctes**, en miroir dans `data-fr.js`/`data-es.js` : **« La Santé » / « La Salud »** conserve l'id `sante` (donc aucun impact sur la progression déjà sauvegardée des utilisateurs sur ce module) et son contenu médical d'origine intact — 21 mots (fiebre, tos, médico, hospital, farmacia, medicamento, vacuna, ambulancia, urgencias, etc.), aucun mot retiré ni modifié ; **« La Sécurité » / « La Seguridad »** est une **rubrique entièrement nouvelle** (nouvel id `securite`, donc démarre avec une progression vierge pour tous les utilisateurs, y compris ceux ayant déjà terminé « La Santé » avant cette session) reprenant les 6 mots de sécurité générale ajoutés lors de l'entrée précédente (¡socorro!/¡ayuda!, el accidente, la policía, los bomberos, el robo, el ladrón/la ladrona), insérée juste après « La Santé » et avant « L'Argent ». 36 → **37 thèmes**, 52 → **53 modules** au total. **`index.html`** : les 2 mentions du Guide passent de 36 à **37**. **`app.js`** : commentaire ARCHITECTURE (37 thèmes) et commentaire de `_renderOtherModeProgress()` (53 modules, 37 thèmes) mis à jour, toujours **sans ajouter ni retirer de ligne** ; valeur de repli `ALL_THEMES.length || 52` → `|| 53`. **Vérifications :** `node --check` sans erreur sur les 3 fichiers JS modifiés ; comptage programmatique confirmant 37 thèmes dans l'ordre attendu (`...met > sante > securite > dinero > viaje...`), un total `ALL_THEMES` de 53 dans les deux modes, 21 mots dans `sante` et 6 mots dans `securite` de part et d'autre. `data-fr.js` : 1 661 → 1 663 lignes (+2), 121 988 → 122 034 octets (+46). `data-es.js` : 1 035 → 1 038 lignes (+3), 117 880 → 117 938 octets (+58). `app.js` : 5 935 → 5 935 lignes (inchangé), delta d'octets négligeable (chiffres 36→37 et 52→53 remplacés à taille de chaîne quasi identique). `index.html` : 1 786 → 1 786 lignes (inchangé). `style.css`, `sw.js`, `manifest.json`, `deploy.yml` non modifiés. Tableau § 2 ci-dessus mis à jour pour `data-fr.js`/`data-es.js` avec les valeurs cumulées des 2 entrées du jour ; reste du périmètre non recalculé identique à l'entrée précédente |
| 26/08/2026 (suite 2) | Ergonomie (Claude Sonnet 5, demande utilisateur) : « comment faire afin de moins effrayer les apprenants car au 1ᵉʳ coup ça peut faire peur de voir autant de rubriques » — 6 pistes présentées, l'utilisateur retient la combinaison recommandée **1+2+3** : regroupement en catégories + catégories repliables + carte « Continuer ». **Écran Sections (`app.js`, §7) :** nouvelle constante `THEME_CATEGORIES` — 5 regroupements thématiques des 37 thèmes du Niveau 1 : 🌱 **Les Bases** (8 : alpha, salut, pres, express, interrog, num, verb, conn), 😊 **Décrire & Ressentir** (7 : heure_calendrier, meteo_climat, adj, emot, fam, col, corps), 🏡 **Vie Quotidienne** (8 : adv, viequot, nour, bois, objets, log, muebles_equipamiento, veth), 🚌 **Sorties & Situations** (8 : rest, orient, lieux, trans, met, sante, securite, dinero), 🌍 **Le Monde** (6 : viaje, pays, ocio, plan, anim, agric) — regroupement purement d'affichage (indexé par `id` de thème, résolu dynamiquement contre `ALL_THEMES` à chaque rendu) : ne touche ni `data-fr.js`/`data-es.js`, ni l'ordre de progression, ni les clés de sauvegarde. Nouvelle fonction `_buildThemeGridHTML(level)` : pour le Niveau 1, enveloppe chaque catégorie dans un `<details class="theme-category">`/`<summary>` natif (repli/dépli sans JS de toggle dédié, accessible clavier + lecteur d'écran par défaut du navigateur) — seules la catégorie « en cours » (1ʳᵉ catégorie contenant un thème pas encore à 3 étoiles) et la suivante sont dépliées par défaut, le reste replié (si tout est terminé, la 1ʳᵉ catégorie s'ouvre par défaut) ; pour le Niveau 2 (16 dialogues), la fonction retombe sur l'ancien comportement — grille simple, jugée déjà assez courte pour ne pas avoir besoin de catégorisation. `renderSections()` délègue désormais à cette fonction le remplissage de `grid1`/`grid2` (au lieu d'un `.map()` direct sur `_buildThemeCard()`). Nouvelles fonctions `_findNextThemeToContinue(level)` (priorité à un thème `state==='progress'`, sinon le 1ᵉʳ `state==='new'`, sinon `null` si le niveau est entièrement à 3 étoiles) et `_buildContinueCard(level)`, qui affichent une carte **« ▶ Continuer »** cliquable (`onclick="openTheme(id)"`) en tête de grille sur les 2 niveaux (`index.html` : nouveaux conteneurs `#continueWrap1`/`#continueWrap2`), avec un message de félicitations à la place si le niveau est déjà entièrement terminé. `style.css` : nouvelles classes `.continue-card` (+ variante `.continue-card--done`) et `.theme-category`/`.theme-category-summary`/`.theme-category-emoji`/`.theme-category-label`/`.theme-category-count`/`.theme-subgrid`, avec surcharges dédiées en mode sombre (texte clair sur fonds translucides adaptés) — **aucune nouvelle variable CSS** : tous les nouveaux styles réutilisent `--c-primary`, `--c-border`, `--c-text-sub`, `--c-done`, `--c-state-complete-bg` déjà déclarées (51 variables/179 déclarations inchangées, recompté par grep, avant et après édition strictement identiques). **Vérifications :** `node --check app.js` sans erreur ; accolades CSS comptées équilibrées (961/961) ; script Node dédié confirmant que les 37 thèmes du Niveau 1 sont couverts par `THEME_CATEGORIES` exactement une fois chacun (aucun doublon, aucun manquant), et que `data-fr.js`/`data-es.js` restent bien en miroir strict des mêmes 37 id dans le même ordre ; simulation de la logique d'ouverture par défaut sur 3 scénarios (utilisateur neuf → catégorie 1 ouverte ; utilisateur à mi-parcours de la 2ᵉ catégorie → catégories 2 et 3 ouvertes, 1/4/5 repliées ; utilisateur ayant tout terminé → repli sur la catégorie 1). **PLAN DU FICHIER d'`app.js` intégralement recalculé** (chaque ancre revérifiée une à une par grep, méthode identique aux recalculs précédents) : au passage, un **décalage de 36 lignes déjà présent avant cette session sur toutes les ancres à partir de §9b** a été détecté et corrigé (écart antérieur non expliqué, probablement un recalcul resté incomplet — non audité plus avant, hors périmètre de cette demande) ; entrée §7 étendue pour documenter les 4 nouveaux éléments (`THEME_CATEGORIES`, `_findNextThemeToContinue()`, `_buildContinueCard()`, `_buildThemeGridHTML()`) et les 2 renvois internes qu'elle contient mis à jour (appel de `_markModulesVisited()` L.2306→L.2445 ; `_hasVisitedModules()` L.4749→L.4936). Nouvelle ligne HISTORIQUE ajoutée dans l'en-tête d'`app.js` (« 26/08/2026 (suite) — Regroupement du Niveau 1... »). Compteur ARCHITECTURE de ligne totale d'`app.js` mis à jour (6 074 lignes) ; comptes « 5 écrans/3 modales » et « 51 variables CSS/179 décl. » inchangés, non retouchés. `app.js` : 5 935 → 6 074 lignes (+139 : +119 pour `THEME_CATEGORIES` + les 3 nouvelles fonctions dans §7, +4 pour l'entrée HISTORIQUE, +16 pour l'extension du texte du PLAN DU FICHIER lui-même), 286 034 → 294 021 octets, 179 → **182** fonctions nommées de premier niveau (recompté par grep). `style.css` : 5 453 → 5 545 lignes (+92), 199 376 → 202 862 octets, 51 variables CSS/179 déclarations (inchangé, confirmé par grep avant/après édition). `index.html` : 1 786 → 1 793 lignes (+7, les 2 conteneurs `#continueWrap1`/`#continueWrap2` + commentaires), 117 191 → 117 584 octets. `data-fr.js`, `data-es.js`, `sw.js`, `manifest.json`, `deploy.yml` non modifiés. `README.md` mis à jour : ligne d'historique ajoutée (26/08/2026, suite 2), et lignes d'architecture `app.js`/`style.css` corrigées au passage (182 fonctions/6 074 lignes, 5 545 lignes — ces deux chiffres étaient déjà légèrement obsolètes avant cette session, 178/5 546 et 5 106, écart antérieur non audité au-delà de cette correction ponctuelle). Tableau § 2 ci-dessus mis à jour pour `app.js`/`style.css`/`index.html` avec les valeurs mesurées cette session ; reste du périmètre (§1, lignes `data-fr.js`/`data-es.js`) non recalculé |
| 27/08/2026 (suite) | Extension (Claude Sonnet 5, demande utilisateur : « pour aider l'apprenant ») du même principe de catégories repliables au **Niveau 2** — jusqu'ici en grille simple (16 dialogues), il passe désormais lui aussi par des catégories, comme le Niveau 1 depuis la veille. **`app.js` (§7) :** nouvelle constante `LEVEL2_CATEGORIES` — 4 regroupements des 16 dialogues (découpage propre aux dialogues plutôt qu'au vocabulaire, donc distinct de `THEME_CATEGORIES`) : 👋 **Premiers Contacts** (3 : salut2, pres2, gustos2), 🧭 **Se Déplacer & Climat** (4 : chemin2, transp2, meteo2, heuretemp2), 🍽️ **Sorties & Consommation** (5 : bar2, resto2, compras2, hotel2, fiesta2), 🏠 **Vie Quotidienne & Santé** (4 : logement2, viequot2, farmacia2, medico2) — émojis volontairement différents de ceux du Niveau 1 (dont 🏡/🚌 déjà pris) pour distinguer les 2 niveaux d'un coup d'œil. `_buildThemeGridHTML(level)` généralisée : choisit `THEME_CATEGORIES` ou `LEVEL2_CATEGORIES` selon le niveau au lieu de retomber sur une grille à plat pour le Niveau 2 — même logique de catégorie « en cours » ouverte par défaut pour les 2 niveaux, aucune duplication de code. Commentaires de section §7 mis à jour dans `app.js`, ainsi que le commentaire des catégories repliables dans `style.css` (mention des 2 niveaux). **`style.css` :** règles `#grid1`/`#grid2` fusionnées en une seule (`#grid1, #grid2 { display:flex; flex-direction:column; }`, sans `gap`) puisque les 2 niveaux utilisent désormais `.theme-category`, qui porte déjà sa propre marge basse — le `gap:10px` que portait encore `#grid2` la veille (pour ses cartes alors à plat) n'a plus lieu d'être. **Vérifications :** `node --check app.js` sans erreur ; accolades CSS comptées équilibrées avant/après (963→962, cohérent avec la fusion de 2 règles en 1) ; script Node dédié confirmant que les 16 dialogues sont couverts par `LEVEL2_CATEGORIES` exactement une fois chacun (aucun doublon, aucun manquant), comparé aux 16 id réels lus dans `LEVEL2_THEMES_FR` (`data-fr.js`) ; miroir FR/ES des 16 id de dialogue confirmé identique par grep ; `THEME_CATEGORIES` (Niveau 1) revérifiée intacte à 37 id sans doublon, non affectée par ce changement ; 51 variables CSS/179 déclarations et 182 fonctions nommées de premier niveau inchangés (recompté par grep, aucune nouvelle variable ni fonction — `LEVEL2_CATEGORIES` est une constante, pas une fonction). **PLAN DU FICHIER d'`app.js` intégralement recalculé** (chaque ancre revérifiée une à une par grep, méthode identique aux recalculs précédents ; le paragraphe explicatif du PLAN DU FICHIER lui-même a cette fois été simplifié pour ne plus annoncer de décalage en nombre de lignes avant la revérification par grep, après qu'une telle annonce s'était invalidée elle-même lors du recalcul précédent du même jour — cf. entrée ci-dessus). Le rattrapage de dérive sur §15b/§15c/§15d (11/18/56 lignes) documenté dans l'entrée précédente reste valable tel quel. `app.js` : 6 118 → 6 142 lignes (+24 : +17 pour `LEVEL2_CATEGORIES` et la généralisation de `_buildThemeGridHTML()`, +5 pour l'entrée HISTORIQUE du jour, +2 pour les ajustements de commentaires de section), 297 125 → 298 796 octets (291,8 Ko). `style.css` : 5 564 lignes (inchangé — la fusion des 2 règles `#grid1`/`#grid2` en 1 seule retire autant de lignes que le commentaire mis à jour en ajoute), 204 068 → 204 127 octets (199,3 Ko). `index.html`, `data-fr.js`, `data-es.js`, `sw.js`, `manifest.json`, `deploy.yml` non modifiés (le regroupement en catégories reste purement un regroupement d'affichage, indexé par `id` de thème existant, sans toucher aux données ni à l'ordre de progression ni aux clés de sauvegarde). `README.md` mis à jour : ligne d'historique ajoutée (27/08/2026, suite), et ligne d'architecture `app.js` corrigée (6 142 lignes). Tableau § 2 ci-dessus mis à jour pour `app.js` avec les valeurs mesurées cette session ; reste du périmètre non recalculé |
| 28/08/2026 | Trois correctifs visuels sur les flashcards (Claude Sonnet 5, demande utilisateur, 2 captures d'écran à l'appui) portant sur trois zones distinctes de l'écran de révision. **(1) Bandeau `.section-label`** — le libellé « Cliquez : Recto Espagnol .../Verso Français » (et son équivalent « Haz clic : ... ») passait systématiquement sur 2 lignes. Cause identifiée : `text-transform: uppercase` combiné à `letter-spacing: 1px`, qui élargissait le texte d'environ 60-100 px selon la région active. Retiré (`text-transform: none`, `letter-spacing: normal`), avec en complément une légère réduction de taille (`.7rem` → `.65rem`) en marge de sécurité. Estimation par rendu de police (DejaVu Sans, proxy du rendu réel faute de navigateur disponible dans cet environnement — Playwright installé mais téléchargement du binaire Chromium bloqué par la configuration réseau) : la quasi-totalité des 7 variantes tient désormais sur une ligne sur un écran de 360 px de large (repère Galaxy A55, déjà utilisé ailleurs dans ce fichier), à l'exception d'« Espagne (Castillan) » — la plus longue, avec son qualificatif entre parenthèses — qui peut encore passer sur 2 lignes dans le pire cas : accepté délibérément, le filet de sécurité `white-space: normal` déjà en place plus loin dans `style.css` (« ne pas couper le nom de la région ») continue justement à protéger ce cas plutôt que de tronquer le texte. **(2) Espace sous le micro** — `.mic-feedback` (zone de résultat de la reconnaissance vocale, vide la plupart du temps) réservait `min-height: 28px`, jugé trop grand entre les boutons audio/micro et l'astuce `.mic-hint` en dessous. Réduit à `14px` plutôt que supprimé, pour limiter (sans l'annuler complètement) le saut de mise en page quand un résultat ✅/🔁 apparaît. **(3) Exemple bilingue entre parenthèses des définitions verbes** — l'utilisateur avait repéré, sur le verso espagnol de la carte « Ser », un mélange français/espagnol dans la définition (« Être (permanent, identité : soy español = je suis espagnol) »), affiché à la même taille énorme (1,9 rem) que le mot principal. Diagnostic : volontaire (nuance nécessaire pour distinguer *ser*/*estar*, tous deux traduits par « être »), donc conservé tel quel — mais l'utilisateur a demandé que la parenthèse d'exemple soit affichée plus petite, à la taille de la traduction. **Correction d'un diagnostic initial erroné** : une vérification plus large (script Node chargeant réellement `data-fr.js`/`data-es.js`, plutôt qu'une extraction approximative par regex sur le texte source) a montré que ce n'est pas seulement `Ser`/`Estar` qui sont concernés, mais **15 définitions par fichier de données** : `Ser`, `Estar`, `Querer`, `Poder`, `Deber`/`Falloir`, `Saber`, `Conocer`, `Venir`, `Acabar`/`Finir`, `Esperar`/`Attendre`, `Pedir`, `Costar`, `Todavía`/`Encore`, `Doler`/`Me duele`, et le nombre `Cent`. Nouvelle fonction `_styleExampleParen(text)` ajoutée dans `app.js` juste après `renderFlash()` : repère par expression régulière la dernière parenthèse contenant un signe « = » (marque d'un exemple traduit, par opposition aux parenthèses purement grammaticales comme « (invariable) », non concernées) et l'entoure de la classe `.translation-sub` déjà utilisée ailleurs dans l'app pour ce rôle (0,75 em, `display:block`, opacité 0,7) plutôt qu'une nouvelle classe dédiée. Appliquée uniquement à la construction de `backContent` dans `renderFlash()` (texte affiché sur `.fc-back-word`, mode Apprendre l'Espagnol) : **volontairement pas appliquée** au texte transmis à `speak()`/`_buildMicButton()` (qui doivent rester du texte brut sans HTML pour la synthèse et la reconnaissance vocale), ni au côté recto du mode Apprendre le Français (où ce même texte passe par `_buildSpeakableHTML()` pour le surlignage mot par mot pendant la lecture — non touché pour ne pas risquer de casser ce surlignage, qui repère les mots par découpage du texte brut plutôt que du HTML). **Vérifications :** `node --check app.js` sans erreur ; accolades et parenthèses de `style.css` comptées équilibrées (971/971 et 1218/1218) ; script Node chargeant réellement les 2 fichiers de données pour confirmer les 15 entrées concernées par fichier et vérifier que `_styleExampleParen()` les transforme toutes correctement sans faux positif sur les parenthèses purement grammaticales des autres thèmes ; `.fc`/`.fc-front`/`.fc-back`/`.fc-conj-line` et `_autosizeFlashCard()` non touchés, donc pas de risque identifié sur le dimensionnement des cartes Verbes (les plus longues, cf. remarque de l'utilisateur). Pas de test dans un navigateur réel possible dans cet environnement : vérifications faites par lecture de code, calcul de largeur de texte par rendu de police, et par le fait qu'aucune des 3 zones touchées ne recoupe le calcul JS d'auto-dimensionnement des cartes. `app.js` : 6 288 → 6 353 lignes (+65 : +34 pour `_styleExampleParen()` et son commentaire, +19 pour la nouvelle entrée HISTORIQUE, +12 pour la note de dérive ajoutée au PLAN DU FICHIER, cf. ci-dessous), 306 641 → 311 320 octets (304,0 Ko), 187 → **188** fonctions nommées de premier niveau (recompté par grep — le chiffre de 182 documenté jusqu'ici dans le README et ce fichier était déjà obsolète avant cette session, écart non audité au-delà de cette correction ponctuelle). `style.css` : 5 626 → 5 647 lignes (+21), 206 843 → 209 599 octets (204,7 Ko), 51 variables CSS/179 déclarations inchangées (aucune nouvelle variable, uniquement des valeurs et des commentaires modifiés sur des déclarations existantes). `index.html`, `data-fr.js`, `data-es.js`, `sw.js`, `manifest.json`, `deploy.yml` non modifiés. **⚠️ PLAN DU FICHIER non recalculé** dans `app.js` (recalculé pour la dernière fois le 27/08/2026) ni dans `style.css` (recalculé pour la dernière fois le 19/07/2026) : une note a été ajoutée en tête des deux fichiers pour le signaler honnêtement plutôt que de laisser les anciens numéros d'ancre sans avertissement — un écart bien plus large que celui introduit par cette seule session a d'ailleurs été mesuré à cette occasion (`renderFlash()` réellement en L.3032 contre L.2893 indiqué dans le plan d'`app.js` avant cette session ; section Modales de `style.css` réellement bien après la ligne 2627 indiquée), signe que plusieurs correctifs ponctuels (dont de nombreux commentaires déjà datés « 28/08/2026 » présents dans le fichier avant même le début de cette session) n'ont pas été suivis d'un recalcul. Un recalcul complet des deux PLAN DU FICHIER reste à faire, hors périmètre de cette demande. `README.md` mis à jour : ligne d'historique ajoutée (28/08/2026), et lignes d'architecture `app.js`/`style.css` corrigées au passage (188 fonctions/6 353 lignes, 5 647 lignes — chiffres déjà obsolètes avant cette session, comme ci-dessus). Tableaux § 1 et § 2 ci-dessus mis à jour avec les valeurs mesurées cette session pour l'ensemble des 6 fichiers de code, dont `data-fr.js`/`data-es.js`/`index.html`/`sw.js` — non modifiés cette session, mais dont les tailles cumulées n'avaient apparemment pas été recalculées depuis le 19/07/2026 non plus (écart entre les « 15 295 lignes » précédemment indiquées en § 1 et les 17 076 lignes mesurées aujourd'hui) |
| 29/08/2026 | Correctif d'affichage **desktop uniquement** (Claude Sonnet 5, demande utilisateur, 2 captures d'écran Windows à l'appui ; mobile explicitement non concerné, « c'est très bien » selon l'utilisateur). **Diagnostic préalable important** : le texte du bandeau `.section-label` visible sur la 2ᵉ capture (« RECTO : ESPAGNOL... — VERSO : FRANÇAIS... - CLIQUEZ POUR RETOURNER ! ») ne correspond à AUCUNE version d'`app.js` connue (ni la version livrée le 28/08, ni même la toute première version uploadée en tout début de conversation, qui produit déjà « Cliquez : Recto Espagnol... - Verso Français... ») — signe que le site effectivement testé par l'utilisateur (déployé sur GitHub Pages, ou servi par un Service Worker au cache non rafraîchi) est **plus ancien** que tous les fichiers échangés dans cette conversation. Signalé à l'utilisateur en réponse plutôt que corrigé silencieusement, avec recommandation de vérifier ce qui est réellement déployé/en cache avant de rejuger les correctifs précédents. **Bug réel identifié et corrigé indépendamment de ce constat**, reproduit avec succès en local (cf. Vérifications) : sur les écrans à contenu court (ex. Flashcards, peu de texte), la mise en page desktop (`@media (min-width:768px)`, ajoutée le 08/07/2026) forçait `.app` et chaque écran actif à `min-height:100vh` tout en alignant la carte en haut du viewport (`body { align-items:flex-start }`) — combinaison qui laissait un grand vide interne d'une couleur très pâle (`--c-bg`, quasi blanche, RVB mesuré 254,253,249 sur la capture utilisateur, à ne pas confondre avec le blanc pur 255,255,255 ni le crème plus visible du pied de page 251,246,227) entre le pied de page et le bas de la carte, avant la barre de navigation basse (fixe, indépendante de la hauteur de la carte). Visuellement indiscernable d'un écran cassé/à moitié chargé. Le commentaire de section annonçait pourtant déjà une carte « centrée verticalement » depuis l'origine de cette fonctionnalité (08/07/2026) : l'implémentation (`flex-start`) ne correspondait donc pas à l'intention documentée — incohérence pré-existante, pas introduite par cette session. **Correctif** dans `style.css`, entièrement contenu dans le bloc `@media (min-width:768px)` (aucun impact mobile, cf. règle de base `.screen{min-height:100dvh}` inchangée) : `body{align-items:flex-start}` → `align-items:center` ; `min-height:100vh` retiré de `.app` ; règle `#app-launcher, .screen.active{min-height:100vh}` remplacée par `.screen.active{min-height:auto}` (uniquement — `#app-launcher` exclu intentionnellement : sa propre règle plus loin dans le fichier, `#app-launcher{min-height:100vh}`, plus spécifique par id, l'emportait de toute façon sur les 2 versions de la règle desktop, avant et après ce correctif ; son layout interne en `justify-content:space-between`, conçu pour occuper toute la hauteur, reste donc inchangé). Résultat : chaque écran desktop prend sa hauteur naturelle et se centre verticalement dans le viewport ; le fond quadrillé de la page (`body`, dégradé de points bleu/gris) devient visible au-dessus ET en dessous de la carte sur les écrans courts, ce qui se lit comme une mise en page voulue plutôt qu'un vide anormal. **Vérifications :** `wkhtmltoimage` (WebKit local, sans accès réseau pour Playwright/Chromium dans cet environnement) utilisé pour un test de rendu réel plutôt qu'une simple lecture de code — reproduction fidèle du bug sur une page de test isolée (structure HTML de `#lesson` + contenu de carte réaliste extrait d'`index.html`, servi localement avec `--enable-local-file-access`), confirmée visuellement avant/après correctif à plusieurs hauteurs de viewport (900 px et 1400 px) ; non-régression vérifiée séparément sur un contenu volontairement long (1400 px de contenu simulé) : la carte dépasse alors naturellement 100vh dans les 2 cas (avant/après), `align-items:center` n'a aucun effet visible sur ce cas (une carte aussi grande que son conteneur ne peut pas être « recentrée », il n'y a pas d'espace superflu à redistribuer) — confirmé sur un montage HTML minimal et contrôlé après qu'une première tentative avec le HTML réel de l'écran Modules a produit un artefact de test sans rapport (extraction par regex ayant cassé l'imbrication des balises, corrigé en isolant le test). Accolades/parenthèses de `style.css` comptées équilibrées après coup (973 accolades). `:has()` (utilisé par la mise en page 2 colonnes des flashcards, §27) non supporté par le moteur WebKit de `wkhtmltoimage` : le repli en 1 colonne documenté dans le code s'est déclenché pendant ces tests, sans rapport avec le correctif du jour. `style.css` : 5 665 → 5 687 lignes (+22, commentaires inclus), 210 605 → 212 337 octets (207,4 Ko). Aucun autre fichier modifié. Tableau § 2 ci-dessus mis à jour pour `style.css` |
| 28/08/2026 (suite) | 2ᵉ passe (Claude Sonnet 5, demande utilisateur, capture d'écran à l'appui) sur le point (3) de l'entrée précédente : la taille `0.75em` de `.translation-sub` appliquée à l'exemple entre parenthèses (~1,4 rem dans le contexte 1,9 rem de `.fc-back-word`/`.fc-front-word`) restait encore trop imposante à côté du mot principal, comme le montre la capture (« Être » suivi de « (permanent, identité : soy español = je suis espagnol) » à une taille très proche du mot lui-même). Nouvelle règle plus spécifique `.fc-back-word .translation-sub, .fc-front-word .translation-sub { font-size: 0.42em; opacity: 0.85; margin-top: 6px; }`, qui l'emporte sur la règle générale `.translation-sub` (spécificité CSS supérieure, 2 sélecteurs de classe imbriqués contre 1 seul) sans la modifier — elle reste inchangée pour ses autres usages (titres, boutons). Taille choisie pour retrouver l'équivalent de `.fc-conj-line` (.78rem, 0.42×1.9rem≈0.8rem), la même famille de texte secondaire déjà utilisée juste en dessous sur la carte pour les traductions de conjugaison (« Je suis », « Tu es »…) — cohérent avec la demande de l'utilisateur (« comme pour les autres verbes où il y a ça aussi »). **Vérifications :** accolades et parenthèses de `style.css` comptées équilibrées (972/972 et 1225/1225) ; règle vérifiée bien plus spécifique que `.translation-sub` seule, donc appliquée correctement sans `!important` (à la différence de la règle `h1 .translation-sub` juste au-dessus, qui en a besoin pour d'autres raisons de cascade propres aux titres). `style.css` : 5 647 → 5 665 lignes (+18, commentaire + règle), 209 599 → 210 605 octets (205,7 Ko). Aucun autre fichier modifié. Tableau § 2 ci-dessus mis à jour pour `style.css` |


